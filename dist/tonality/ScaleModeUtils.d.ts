import { Scale } from './Scale';
export declare class ScaleModeUtils {
    private constructor();
    static getMode(sourceScale: Scale, n: number): Scale;
    static getRotatedScale(sourceScale: Scale, n: number): Scale;
}
